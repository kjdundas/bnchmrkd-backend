# Copy to .env and paste the pooled connection string from
# Supabase -> Project Settings -> Database -> Connection string -> URI
# (the one with the password in it). .env is gitignored.
SUPABASE_DB_URL=postgresql://postgres.bmbqjyrhzusidxmfrssi:PASSWORD@aws-0-ap-south-1.pooler.supabase.com:5432/postgres
